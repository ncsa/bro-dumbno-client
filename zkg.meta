[package]
description = Adds functionality for bro to talk with dumbno
tags = zeek plugin, zeekctl plugin, dumbno
plugin_dir = build/NCSA_Dumbno.tgz
build_command = ./configure && make
depends =
  zkg >=2.0
  zeek >=4.0.0
